[package]
name = "libcap"
version = "2.76"
description = "TODO"

[sources.libcap]
format = "tar.gz"
file = "libcap-{version}.{format}"
mirrors = [ "https://git.kernel.org/pub/scm/libs/libcap/libcap.git/snapshot/{file}",]
