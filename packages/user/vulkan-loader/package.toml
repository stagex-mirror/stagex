[package]
name = "vulkan-loader"
version = "1.4.325"
description = "Vulkan Installable Client Driver (ICD) Loader"

[sources.vulkan-loader]
format = "tar.gz"
file = "vulkan-loader-sdk-{version}.{format}"
mirrors = [ "https://github.com/khronosgroup/vulkan-loader/archive/refs/tags/vulkan-sdk-{version}/{file}",]
