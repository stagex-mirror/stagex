FROM stagex/core-busybox AS core-busybox
FROM stagex/core-musl AS core-musl
FROM stagex/core-curl AS core-curl
FROM stagex/core-python AS core-python
FROM stagex/user-keylime-verifier AS user-keylime-verifier

FROM scratch AS package
COPY --from=core-busybox . /
COPY --from=core-musl . /
COPY --from=core-curl . /
COPY --from=core-python . /
COPY --from=user-keylime-verifier . /
COPY src/verify-enclave.sh /usr/bin/verify-enclave
RUN chmod +x /usr/bin/verify-enclave
