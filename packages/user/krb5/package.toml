[package]
name = "krb5"
version = "1.22-final"
description = "TODO"

[sources.krb5]
format = "tar.gz"
file = "krb5-{version}.{format}"
mirrors = [ "https://kerberos.org/dist/krb5/{version_major_minor}/{file}",]
