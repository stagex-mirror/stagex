FROM stagex/pallet-rust AS build
ARG VERSION
ARG TARGETARCH
ADD fetch/cloud-seed-${VERSION}.tar.gz .
WORKDIR /cloud-seed-0.2.0
ENV RUSTFLAGS='-C codegen-units=1 -C target-feature=+crt-static'
RUN cargo fetch
RUN --network=none <<-EOF
  set -eux
	ARCH=$(uname -m)
  cargo build \
    --frozen \
    --release \
    --target "${ARCH}-unknown-linux-musl"
		install -vDm755 -t /rootfs/usr/bin target/${ARCH}-unknown-linux-musl/release/cloud-seed
EOF
FROM stagex/core-filesystem AS package
COPY --from=build /rootfs/ /
