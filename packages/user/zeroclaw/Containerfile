FROM stagex/pallet-rust AS build
ARG VERSION
ADD fetch/zeroclaw-v${VERSION}.tar.gz .
WORKDIR /zeroclaw-${VERSION}
RUN cargo fetch --locked
ENV RUSTFLAGS="-C codegen-units=1 -C target-feature=+crt-static"
RUN --network=none <<-EOF
	cargo build \
		--frozen \
		--release \
		--target "${ARCH}-unknown-linux-musl" \
		--locked
	install -Dm755 "target/${ARCH}-unknown-linux-musl/release/zeroclaw" /rootfs/usr/bin/zeroclaw
	install -Dm644 -t /rootfs/usr/share/licenses/zeroclaw/ LICENSE-APACHE LICENSE-MIT
EOF

FROM stagex/core-filesystem AS package
COPY --from=build /rootfs/ /
