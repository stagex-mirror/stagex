FROM stagex/pallet-python AS build
ARG VERSION
COPY --from=stagex/core-openssl . /
ADD fetch/acme-tiny-${VERSION}.tar.gz .
WORKDIR /acme-tiny-${VERSION}
RUN --network=none <<-EOF
  set -eu
	install -vDm755 -t /rootfs/usr/bin/acme-tiny 755 acme_tiny.py
	install -vDm644 -t /rootfs/usr/share/licenses/acme-tiny LICENSE
EOF

FROM stagex/core-filesystem AS package
COPY --from=build /rootfs /
