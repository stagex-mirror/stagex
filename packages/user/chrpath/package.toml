[package]
name = "chrpath"
version = "0.18"
description = "TODO"

[sources.chrpath]
file = "chrpath-{version}.tar.gz"
mirrors = [ "https://alioth-archive.debian.org/releases/chrpath/chrpath/{version}/{file}",]
