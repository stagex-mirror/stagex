[package]
name = "enclave"
version = "2026.03.0"
description = "Minimal immutable distro — erofs root, UKI firmware-direct boot (live layout, disk image, or QEMU VM)"
subpackages = [
  "enclave-dev",
  "enclave-dev-img",
  "enclave-dev-vm",
]
