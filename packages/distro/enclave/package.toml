[package]
name = "enclave"
version = "2026.03.0"
description = "Minimal immutable distro — squashfs root, GRUB EFI, dual-mode boot (live or persistent)"
subpackages = ["enclave-dev", "enclave-guest-offline", "enclave-guest-online", "enclave-guest-online-dev"]
