# Keylime configuration for rust-keylime agent
# This is used by the S52keylime-agent init script

[agent]
# Agent runs on port 9002 (rust-keylime default)
# Accessible on 0.0.0.0 internally, or 8891 externally via TCP redirect

[log]
log_level = INFO
log_file = /var/log/keylime/keylime-agent.log
